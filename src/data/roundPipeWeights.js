// Placeholder functions for pipe weight calculations
// These return null to force the use of calculated weights instead of standard lookup tables

export const getRoundPipeWeightPer20ft = (size, thickness) => {
  // Return null to use calculated weight
  return null;
};

export const getSquarePipeWeightPer20ft = (size, thickness) => {
  // Return null to use calculated weight
  return null;
};